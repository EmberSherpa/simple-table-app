import { findSinger } from '../../../helpers/find-singer';
import { module, test } from 'qunit';

module('Unit | Helper | find singer');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = findSinger(42);
  assert.ok(result);
});
